# URL de base de votre API backend
# Le préfixe VITE_ est important pour que Vite expose cette variable
# à votre code frontend (accessible via import.meta.env.VITE_API_URL).
VITE_API_URL=http://localhost:5001/api